// Network layer - P2P connection management 
// TODO: Implement PeerJS connection and messaging 
 
let peer = null; 
let connection = null; 
 
export function initializePeer(onReady, onError) { 
    // TODO: Initialize PeerJS 
} 
 
export function hostGame(onConnected) { 
    // TODO: Set up host 
} 
 
export function joinGame(roomCode, onConnected, onError) { 
    // TODO: Connect to host 
} 
 
export function sendData(data) { 
    // TODO: Send data to peer 
} 
 
export function onData(callback) { 
    // TODO: Set data receive callback 
} 
